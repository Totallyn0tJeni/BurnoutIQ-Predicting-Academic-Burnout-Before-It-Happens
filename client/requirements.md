## Packages
recharts | Data visualization for burnout trends
framer-motion | Smooth animations for page transitions and UI elements
date-fns | Date formatting and manipulation
react-day-picker | Calendar component for date selection
clsx | Utility for conditional classes
tailwind-merge | Utility for merging tailwind classes

## Notes
Authentication is handled via Replit Auth (useAuth hook).
API endpoints are defined in shared/routes.ts.
Charts require responsive containers.
